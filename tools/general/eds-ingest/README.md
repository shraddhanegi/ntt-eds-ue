# EDS URL Ingest (Tools > General)

The author UI has moved into the installable package:

**`tools/ingest/ui/`**

Start it with:

```bash
npm run ingest:ui
```

Open: **http://localhost:3002/**

For installation in other environments (npm package, Docker, CI), see **[tools/ingest/INSTALL.md](../ingest/INSTALL.md)**.
